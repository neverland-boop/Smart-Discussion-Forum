package storage;

/**
 * Out of scope for Sprint 1. Belongs to Bidal's offline-queueing work
 * (saveOffline() / ISyncManager, Sprint 3), which handles pending actions
 * created while disconnected. Intentionally left as a stub for now.
 */
public class OfflineStorage {
}
